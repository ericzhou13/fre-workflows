apiVersion: 1

datasources:
  - name: gaea_db
    type: frser-sqlite-datasource
    access: proxy
    editable: true
    jsonData:
      path: /mnt/cylc-data/gaea_db
    uid: gaea_sqlite

  - name: pp_db
    type: frser-sqlite-datasource
    access: proxy
    editable: true
    jsonData:
      path: /mnt/cylc-data/pp_db
    uid: pp_sqlite